export interface LaravelCheatSheetItem {
  name: string;
  description: string;
  example: string;
  category: 'Artisan Commands' | 'Routing' | 'Blade Templates' | 'Eloquent ORM' | 'Database & Migrations' | 'Controllers' | 'Validation' | 'Middleware' | 'Authentication' | 'Collections' | 'File Storage' | 'Queues' | 'Task Scheduling' | 'Testing';
}

export const laravelData: LaravelCheatSheetItem[] = [
  // --- Artisan Commands ---
  {
    name: 'php artisan serve',
    description: 'Starts the built-in PHP development server for your Laravel application.',
    example: 'php artisan serve --port=8080',
    category: 'Artisan Commands',
  },
  {
    name: 'php artisan tinker',
    description: 'Starts an interactive REPL (Read-Eval-Print Loop) for your application, allowing you to run any application code.',
    example: 'php artisan tinker\n>>> App\\Models\\User::find(1);',
    category: 'Artisan Commands',
  },
  {
    name: 'php artisan make:model',
    description: 'Creates a new Eloquent model class. The -m, -c, -r, -f flags will also create a migration, controller, resource controller, and factory.',
    example: 'php artisan make:model Post -mcrf',
    category: 'Artisan Commands',
  },
  {
    name: 'php artisan make:controller',
    description: 'Creates a new controller class. The --resource flag will create a controller with methods for all resource routes.',
    example: 'php artisan make:controller PostController --resource',
    category: 'Artisan Commands',
  },
  {
    name: 'php artisan make:migration',
    description: 'Creates a new database migration file. Use `--create` to pre-fill the schema for a new table.',
    example: 'php artisan make:migration create_products_table --create=products',
    category: 'Artisan Commands',
  },
  {
    name: 'php artisan make:seeder',
    description: 'Creates a new database seeder class to populate your database with data.',
    example: 'php artisan make:seeder ProductSeeder',
    category: 'Artisan Commands',
  },
  {
    name: 'php artisan make:factory',
    description: 'Creates a new model factory to generate fake data for your models, typically used in testing and seeding.',
    example: 'php artisan make:factory ProductFactory --model=Product',
    category: 'Artisan Commands',
  },
  {
    name: 'php artisan make:request',
    description: 'Creates a new form request class, which is used to contain validation logic for a specific request.',
    example: 'php artisan make:request StoreProductRequest',
    category: 'Artisan Commands',
  },
  {
    name: 'php artisan make:middleware',
    description: 'Creates a new middleware class.',
    example: 'php artisan make:middleware CheckAge',
    category: 'Artisan Commands',
  },
  {
    name: 'php artisan make:test',
    description: 'Creates a new test class. The --unit flag creates a unit test.',
    example: 'php artisan make:test UserTest\nphp artisan make:test PostTest --unit',
    category: 'Artisan Commands',
  },
  {
    name: 'php artisan make:job',
    description: 'Creates a new job class, which can be queued.',
    example: 'php artisan make:job ProcessPodcast',
    category: 'Artisan Commands',
  },
  {
    name: 'php artisan migrate',
    description: 'Runs all of your outstanding database migrations.',
    example: 'php artisan migrate\nphp artisan migrate:rollback // Rollback the last migration\nphp artisan migrate:fresh --seed // Drop all tables and re-run all migrations with seeders',
    category: 'Artisan Commands',
  },
  {
    name: 'php artisan db:seed',
    description: 'Seeds the database with records defined in your seeder classes. Can specify a single class to run.',
    example: 'php artisan db:seed\nphp artisan db:seed --class=ProductSeeder',
    category: 'Artisan Commands',
  },
  {
    name: 'php artisan route:list',
    description: 'Lists all registered routes in your application. Useful for debugging.',
    example: 'php artisan route:list\nphp artisan route:list --path=api',
    category: 'Artisan Commands',
  },
  {
    name: 'php artisan config:cache',
    description: 'Creates a cache file for faster configuration loading. Run this in production.',
    example: 'php artisan config:cache\nphp artisan config:clear // Clear the config cache',
    category: 'Artisan Commands',
  },
  {
    name: 'php artisan route:cache',
    description: 'Creates a route cache file for faster route registration. Run this in production.',
    example: 'php artisan route:cache\nphp artisan route:clear // Clear the route cache',
    category: 'Artisan Commands',
  },
  {
    name: 'php artisan view:cache',
    description: 'Compiles all Blade views into plain PHP files for improved performance. Run this in production.',
    example: 'php artisan view:cache\nphp artisan view:clear // Clear all compiled views',
    category: 'Artisan Commands',
  },
  {
    name: 'php artisan optimize',
    description: 'A convenient command to cache the configuration, routes, events, and views.',
    example: 'php artisan optimize',
    category: 'Artisan Commands',
  },
  {
    name: 'php artisan optimize:clear',
    description: 'A convenient command to clear all cached files (config, routes, views, events).',
    example: 'php artisan optimize:clear',
    category: 'Artisan Commands',
  },
  {
    name: 'php artisan down / up',
    description: 'Puts the application into maintenance mode (`down`) or brings it out of maintenance mode (`up`).',
    example: 'php artisan down --secret="bypass-key"\nphp artisan up',
    category: 'Artisan Commands',
  },
  {
    name: 'php artisan vendor:publish',
    description: 'Publishes any publishable assets from vendor packages (e.g., configuration, views, migrations).',
    example: 'php artisan vendor:publish --provider="Some\\Package\\ServiceProvider"',
    category: 'Artisan Commands',
  },
  {
    name: 'php artisan queue:work',
    description: 'Starts a worker to process jobs on the queue.',
    example: 'php artisan queue:work\nphp artisan queue:work --queue=emails',
    category: 'Artisan Commands',
  },
  {
    name: 'php artisan schedule:run',
    description: 'Runs the scheduled commands. This is typically configured to run every minute via a server cron job.',
    example: '* * * * * cd /path-to-your-project && php artisan schedule:run >> /dev/null 2>&1',
    category: 'Artisan Commands',
  },

  // --- Routing ---
  {
    name: 'Route::get()',
    description: 'Defines a route that responds to GET HTTP requests.',
    example: `// routes/web.php\nuse App\\Http\\Controllers\\PostController;\n\nRoute::get('/posts', [PostController::class, 'index']);`,
    category: 'Routing',
  },
  {
    name: 'Route::post()',
    description: 'Defines a route that responds to POST HTTP requests.',
    example: `Route::post('/posts', [PostController::class, 'store']);`,
    category: 'Routing',
  },
    {
    name: 'Other HTTP Verbs',
    description: 'Define routes for other HTTP verbs like PUT, PATCH, DELETE, and OPTIONS.',
    example: `Route::put('/posts/{id}', [PostController::class, 'update']);\nRoute::patch('/posts/{id}', [PostController::class, 'update']);\nRoute::delete('/posts/{id}', [PostController::class, 'destroy']);`,
    category: 'Routing',
  },
  {
    name: 'Route::match() & Route::any()',
    description: '`match` registers a route that responds to multiple specified HTTP verbs. `any` registers a route that responds to all HTTP verbs.',
    example: `Route::match(['get', 'post'], '/', function () { ... });\n\nRoute::any('/contact', function () { ... });`,
    category: 'Routing',
  },
  {
    name: 'Route::view() & Route::redirect()',
    description: '`view` creates a simple route that returns a view without needing a controller. `redirect` creates a route that redirects to another URI.',
    example: `Route::view('/welcome', 'welcome');\n\nRoute::redirect('/here', '/there', 301);`,
    category: 'Routing',
  },
  {
    name: 'Route Parameters',
    description: 'Defines routes that capture segments of the URI. Parameters can be required or optional (by adding a `?`).',
    example: `// Required parameter\nRoute::get('/posts/{id}', function ($id) { ... });\n\n// Optional parameter\nRoute::get('/user/{name?}', function ($name = 'John') { ... });`,
    category: 'Routing',
  },
  {
    name: 'Route Parameter Constraints',
    description: 'Use the `where` method to constrain the format of route parameters using regular expressions.',
    example: `Route::get('/user/{id}', function ($id) { ... })->where('id', '[0-9]+');\n\nRoute::get('/user/{name}', function ($name) { ... })->where('name', '[A-Za-z]+');`,
    category: 'Routing',
  },
  {
    name: 'Implicit Route Model Binding',
    description: 'Laravel automatically resolves Eloquent models type-hinted in a route\'s callback or controller method. The variable name must match the route segment name (e.g., `{user}`).',
    example: `use App\\Models\\User;\n\n// Laravel automatically finds the User with the given ID\nRoute::get('/users/{user}', function (User $user) {\n    return $user->email;\n});`,
    category: 'Routing',
  },
  {
    name: 'Named Routes',
    description: 'Assigning a name to a route makes it convenient to generate URLs or redirects for that specific route.',
    example: `Route::get('/user/profile', [ProfileController::class, 'show'])->name('profile.show');`,
    category: 'Routing',
  },
  {
    name: 'URL Generation (route() helper)',
    description: 'The `route` helper function can be used to generate URLs to named routes. You can pass parameters as the second argument.',
    example: `// Generates URL: /user/profile\n$url = route('profile.show');\n\n// Generates URL: /posts/1\n$url = route('posts.show', ['id' => 1]);`,
    category: 'Routing',
  },
  {
    name: 'Route::resource()',
    description: 'Defines a resource controller route, which automatically creates routes for common CRUD operations (index, create, store, show, edit, update, destroy).',
    example: `Route::resource('photos', PhotoController::class);`,
    category: 'Routing',
  },
  {
    name: 'Route Groups',
    description: 'Allows you to apply attributes (like middleware, prefixes, or name prefixes) to a group of routes.',
    example: `Route::middleware(['auth'])\n    ->prefix('admin')\n    ->name('admin.')\n    ->group(function () {\n        Route::get('/dashboard', ...)->name('dashboard'); // Route name is 'admin.dashboard'\n    });`,
    category: 'Routing',
  },
  {
    name: 'Fallback Routes',
    description: 'Define a route that will be executed when no other route matches the incoming request. Typically used for handling 404 pages.',
    example: `Route::fallback(function () {\n    return view('errors.404');\n});`,
    category: 'Routing',
  },
  {
    name: 'Rate Limiting',
    description: 'Apply rate limiting middleware to a route or group of routes to limit the number of requests from an IP address in a given amount of time.',
    example: `// Allows 60 requests per minute\nRoute::middleware('throttle:60,1')->group(function () {\n    Route::get('/user', ...);\n});`,
    category: 'Routing',
  },
  
  // --- Middleware ---
  {
    name: 'Creating Middleware',
    description: 'Generate a new middleware using the `make:middleware` Artisan command.',
    example: 'php artisan make:middleware EnsureTokenIsValid',
    category: 'Middleware',
  },
  {
    name: '"Before" Middleware Logic',
    description: 'The core logic of a middleware resides in its `handle` method. Logic before `$next($request)` runs before the request is handled by the controller.',
    example: `// app/Http/Middleware/EnsureTokenIsValid.php\npublic function handle(Request $request, Closure $next)\n{\n    if ($request->input('token') !== 'my-secret-token') {\n        return redirect('home');\n    }\n\n    return $next($request);\n}`,
    category: 'Middleware',
  },
  {
    name: '"After" Middleware Logic',
    description: 'To perform a task *after* the request has been handled by the application, execute your logic after calling the `$next` closure. This allows you to modify the response before it is sent back to the browser.',
    example: `// app/Http/Middleware/AddCustomHeader.php\npublic function handle(Request $request, Closure $next)\n{\n    $response = $next($request);\n\n    $response->header('X-Custom-Header', 'My-Value');\n\n    return $response;\n}`,
    category: 'Middleware',
  },
  {
    name: 'Registering Middleware',
    description: 'Register middleware in `app/Http/Kernel.php`. It can be global (`$middleware`), for web/api routes (`$middlewareGroups`), or given an alias for route-specific use (`$middlewareAliases`).',
    example: `// app/Http/Kernel.php\nprotected $middlewareAliases = [\n    'auth' => \\App\\Http\\Middleware\\Authenticate::class,\n    'token' => \\App\\Http\\Middleware\\EnsureTokenIsValid::class,\n];\n\n// Usage in routes/web.php\nRoute::get('/profile', ...)->middleware('token');`,
    category: 'Middleware',
  },
  {
    name: 'Middleware Parameters',
    description: 'Middleware can receive additional parameters. Parameters are passed in the route definition after the middleware\'s name, separated by a colon `:`.',
    example: `// app/Http/Middleware/CheckRole.php\npublic function handle(Request $request, Closure $next, string $role)\n{\n    if (! $request->user()->hasRole($role)) {\n        // Redirect...\n    }\n    return $next($request);\n}\n\n// routes/web.php\nRoute::get('/posts/create', ...)->middleware('role:editor');`,
    category: 'Middleware',
  },
  {
    name: 'Middleware Groups',
    description: 'You can group several middleware under a single key in `app/Http/Kernel.php` to make them easier to apply to routes.',
    example: `// app/Http/Kernel.php\nprotected $middlewareGroups = [\n    'admin' => [\n        \\App\\Http\\Middleware\\Authenticate::class,\n        \\App\\Http\\Middleware\\CheckRole::class . ':admin',\n    ]\n];\n\n// routes/web.php\nRoute::prefix('admin')->middleware('admin')->group(function () { ... });`,
    category: 'Middleware',
  },
  {
    name: 'Controller Middleware',
    description: 'Middleware can be assigned directly within a controller\'s constructor. This is useful for scoping middleware to specific controller actions using `only()` or `except()`.',
    example: `// app/Http/Controllers/PostController.php\npublic function __construct()\n{\n    $this->middleware('auth');\n    $this->middleware('subscribed')->except('store');\n}`,
    category: 'Middleware',
  },
  {
    name: 'Terminable Middleware',
    description: 'If a middleware defines a `terminate` method, it will be called *after* the response has been sent to the browser. This is useful for tasks like logging or writing session data.',
    example: `// app/Http/Middleware/LogRequest.php\npublic function handle(Request $request, Closure $next)\n{\n    return $next($request);\n}\n\npublic function terminate($request, $response)\n{\n    Log::info('Request handled', ['request' => $request->all()]);\n}`,
    category: 'Middleware',
  },
  {
    name: 'Middleware Priority',
    description: 'To control the global execution order of middleware, set the `$middlewarePriority` property in `app/Http/Kernel.php`. Middleware in this array will run first.',
    example: `// app/Http/Kernel.php\nprotected $middlewarePriority = [\n    \\Illuminate\\Cookie\\Middleware\\EncryptCookies::class,\n    \\Illuminate\\Session\\Middleware\\StartSession::class,\n    // ... other core middleware\n    \\App\\Http\\Middleware\\MyCustomMiddleware::class,\n];`,
    category: 'Middleware',
  },

  // --- Controllers ---
  {
    name: 'Basic Controller',
    description: 'A class that groups related request handling logic. Methods can be mapped to routes.',
    example: `class PostController extends Controller\n{\n  public function index()\n  {\n    $posts = Post::all();\n    return view('posts.index', ['posts' => $posts]);\n  }\n}`,
    category: 'Controllers',
  },
  {
    name: 'Resource Controller',
    description: 'A controller generated with `php artisan make:controller --resource` that contains stubbed methods for all RESTful actions. Use `--api` to exclude `create` and `edit` methods for APIs.',
    example: `// php artisan make:controller PostController --resource --api\nclass PostController extends Controller\n{\n  public function index() { /*...*/ }\n  public function store(Request $request) { /*...*/ }\n  public function show(Post $post) { /*...*/ }\n  public function update(Request $request, Post $post) { /*...*/ }\n  public function destroy(Post $post) { /*...*/ }\n}`,
    category: 'Controllers',
  },
  {
    name: 'Single Action Controller',
    description: 'A controller that handles a single action. It contains a single `__invoke` method. You can generate one using the `--invokable` flag.',
    example: `// php artisan make:controller ShowUserProfile --invokable\n\nclass ShowUserProfile extends Controller\n{\n    public function __invoke(User $user)\n    {\n        return view('user.profile', ['user' => $user]);\n    }\n}\n\n// In routes/web.php\nRoute::get('/user/{user}', ShowUserProfile::class);`,
    category: 'Controllers',
  },
  {
    name: 'Dependency Injection & Request Object',
    description: 'Laravel uses its service container to automatically resolve and inject dependencies (like the `Illuminate\\Http\\Request` object) into controller methods.',
    example: `use Illuminate\\Http\\Request;\n\npublic function store(Request $request)\n{\n    // Get all input data as an array\n    $allData = $request->all();\n\n    // Get a specific input value\n    $name = $request->input('name');\n\n    // Check if a value is present on the request\n    if ($request->has('email')) { ... }\n}`,
    category: 'Controllers',
  },
   {
    name: 'Route Model Binding',
    description: 'Implicitly inject Eloquent models directly into your controller methods. The type-hinted variable name (`$post`) must match the route segment name (`{post}`). Laravel will automatically fetch the model from the database.',
    example: `// Route in routes/web.php: Route::get('/posts/{post}', [PostController::class, 'show']);\n\nuse App\\Models\\Post;\n\npublic function show(Post $post) // Laravel automatically finds the Post with the given ID\n{\n    return view('posts.show', ['post' => $post]);\n}`,
    category: 'Controllers',
  },
  {
    name: 'Form Request Validation',
    description: 'For complex validation, create a Form Request class using `php artisan make:request`. Type-hinting it in the controller method will automatically validate the request before your method is called. If validation fails, a redirect response is automatically generated.',
    example: `// Run: php artisan make:request StorePostRequest\n\n// In app/Http/Requests/StorePostRequest.php\npublic function rules(): array\n{\n    return ['title' => 'required|max:255', 'body' => 'required'];\n}\n\n// In PostController.php\nuse App\\Http\\Requests\\StorePostRequest;\n\npublic function store(StorePostRequest $request)\n{\n    // The request is valid, access validated data:\n    $validated = $request->validated();\n\n    Post::create($validated);\n    return redirect('/posts');\n}`,
    category: 'Controllers',
  },
  {
    name: 'Returning JSON Responses',
    description: 'The `response()->json()` method will automatically set the `Content-Type` header to `application/json` and convert the given array or Eloquent model to JSON.',
    example: `use App\\Models\\User;\n\npublic function show(User $user)\n{\n    return response()->json($user);\n}`,
    category: 'Controllers',
  },
  {
    name: 'Returning Redirects',
    description: 'Use the `redirect()` helper to generate a redirect response. You can redirect to a named route, a specific path, or back to the previous location with flash data.',
    example: `public function store(Request $request)\n{\n    // ... process data\n\n    return redirect()->route('posts.index')->with('success', 'Post created!');\n    // return back()->withInput();\n}`,
    category: 'Controllers',
  },
   {
    name: 'Returning File Downloads',
    description: 'The `response()->download()` method can be used to generate a response that forces the user\'s browser to download the file at a given path.',
    example: `public function downloadInvoice(Invoice $invoice)\n{\n    return response()->download(storage_path('app/invoices/' . $invoice->id . '.pdf'));\n}`,
    category: 'Controllers',
  },

  // --- Blade Templates ---
  {
    name: '{{ $variable }}',
    description: 'Displays the value of a variable. The content is automatically escaped to prevent XSS attacks.',
    example: `<h1>{{ $post->title }}</h1>`,
    category: 'Blade Templates',
  },
  {
    name: '{!! $variable !!}',
    description: 'Displays unescaped data. Use with extreme caution and only with trusted content.',
    example: `<div class="content">{!! $post->html_content !!}</div>`,
    category: 'Blade Templates',
  },
  {
    name: '@json',
    description: 'Safely renders a PHP array or object as a JSON string, correctly quoted and escaped for use in JavaScript.',
    example: `<script>\n  const user = @json($user);\n</script>`,
    category: 'Blade Templates',
  },
  {
    name: 'Blade Comments',
    description: 'Blade comments are not included in the final HTML rendered by your application, unlike HTML comments.',
    example: `{{-- This comment will not be present in the rendered HTML --}}`,
    category: 'Blade Templates',
  },
  {
    name: '@if / @foreach',
    description: 'Blade directives for common PHP control structures.',
    example: `@if ($posts->count() > 0)\n  @foreach ($posts as $post)\n    <p>{{ $post->title }}</p>\n  @endforeach\n@else\n  <p>No posts found.</p>\n@endif`,
    category: 'Blade Templates',
  },
  {
    name: '@forelse / @empty',
    description: 'A loop similar to `@foreach` but allows you to define a template to render if the given array is empty.',
    example: `@forelse ($users as $user)\n  <li>{{ $user->name }}</li>\n@empty\n  <p>No users</p>\n@endforelse`,
    category: 'Blade Templates',
  },
  {
    name: 'The Loop Variable ($loop)',
    description: 'Inside a `@foreach` or `@forelse` loop, the `$loop` variable is available, providing access to useful metadata about the loop (e.g., `$loop->first`, `$loop->last`, `$loop->iteration`).',
    example: `@foreach ($users as $user)\n  <div @if ($loop->first) class="first" @endif>\n    <p>{{ $loop->iteration }}: {{ $user->name }}</p>\n  </div>\n@endforeach`,
    category: 'Blade Templates',
  },
  {
    name: '@auth / @guest',
    description: 'Conditional directives to show content based on whether the user is authenticated (`@auth`) or a guest (`@guest`).',
    example: `@auth\n  // The user is authenticated...\n@elseguest\n  // The user is not authenticated...\n@endauth`,
    category: 'Blade Templates',
  },
  {
    name: '@yield / @section / @extends',
    description: 'Used for template inheritance. `@extends` specifies the parent layout. `@yield` defines a section in a layout, and `@section` provides content for that section in a child view.',
    example: `// layout.blade.php\n<div class="container">@yield('content')</div>\n\n// page.blade.php\n@extends('layout')\n@section('content')<p>Page content.</p>@endsection`,
    category: 'Blade Templates',
  },
  {
    name: '@include',
    description: 'Includes a Blade view from within another view. You can pass data to the included view.',
    example: `// Include a partial view\n@include('partials.header')\n\n// Include with data\n@include('partials.user-card', ['user' => $user])`,
    category: 'Blade Templates',
  },
  {
    name: '@stack / @push',
    description: '`@stack` defines a location in a layout where you can inject content. `@push` adds content to that stack from a child view. Useful for page-specific scripts or styles.',
    example: `// layout.blade.php\n@stack('scripts')\n\n// page.blade.php\n@push('scripts')\n  <script src="/page-script.js"></script>\n@endpush`,
    category: 'Blade Templates',
  },
  {
    name: 'Blade Components & Slots',
    description: 'Reusable, component-based template sections denoted by an `x-` prefix. Pass content via the default `$slot` variable or into named slots.',
    example: `// alert.blade.php\n<div class="alert">\n  <div class="title">{{ $title }}</div>\n  {{ $slot }}\n</div>\n\n// Usage\n<x-alert>\n  <x-slot:title>Warning</x-slot:title>\n  This is the main content of the alert.\n</x-alert>`,
    category: 'Blade Templates',
  },
  {
    name: '@csrf',
    description: 'Generates a hidden input field containing a CSRF (Cross-Site Request Forgery) token. This is essential for securing POST, PUT, PATCH, or DELETE requests.',
    example: `<form method="POST" action="/profile">\n  @csrf\n  ...\n</form>`,
    category: 'Blade Templates',
  },
  {
    name: '@method',
    description: 'Used to spoof HTTP verbs. Since HTML forms only support GET and POST, this directive generates a hidden input to specify PUT, PATCH, or DELETE methods.',
    example: `<form method="POST" action="/posts/1">\n  @csrf\n  @method('DELETE')\n  <button>Delete Post</button>\n</form>`,
    category: 'Blade Templates',
  },

  // --- Eloquent ORM ---
  {
    name: 'Model::all()',
    description: 'Retrieves all records from the model\'s associated database table.',
    example: `$users = User::all();`,
    category: 'Eloquent ORM',
  },
  {
    name: 'Model::find()',
    description: 'Retrieves a single record by its primary key.',
    example: `$user = User::find(1);`,
    category: 'Eloquent ORM',
  },
  {
    name: 'Model::findOrFail() / firstOrFail()',
    description: 'Like `find()` and `first()`, but they will throw a `ModelNotFoundException` if no result is found, automatically triggering a 404 HTTP response in web routes.',
    example: `$user = User::findOrFail(1);\n$post = Post::where('slug', $slug)->firstOrFail();`,
    category: 'Eloquent ORM',
  },
  {
    name: 'Model::where()',
    description: 'Adds a "where" clause to the query. Use `get()` to execute the query and retrieve results, or `first()` to get a single record.',
    example: `$activeUsers = User::where('active', 1)->orderBy('name')->get();`,
    category: 'Eloquent ORM',
  },
  {
    name: 'Aggregates (count, max, etc.)',
    description: 'Eloquent provides aggregate methods like `count`, `sum`, `max`, `min`, and `avg` to perform calculations on your query results.',
    example: `$userCount = User::count();\n$maxPrice = Product::where('active', 1)->max('price');`,
    category: 'Eloquent ORM',
  },
  {
    name: 'Creating & Saving Models',
    description: 'Create a new model instance and save it to the database, or use the `create` method for mass assignment.',
    example: `// Method 1: New instance\n$post = new Post;\n$post->title = 'New Post';\n$post->save();\n\n// Method 2: Mass assignment (requires fillable property on model)\n$post = Post::create(['title' => 'New Post']);`,
    category: 'Eloquent ORM',
  },
  {
    name: 'Mass Assignment ($fillable / $guarded)',
    description: 'To protect against mass-assignment vulnerabilities, you must define either a `$fillable` (allowed) or `$guarded` (disallowed) property on your model when using `Model::create()` or `Model::update()`.',
    example: `// In Post model\nprotected $fillable = ['title', 'content'];\n// OR\nprotected $guarded = ['id', 'created_at'];`,
    category: 'Eloquent ORM',
  },
  {
    name: 'Updating Models',
    description: 'You can update a model by retrieving it, changing its properties, and calling `save()`. Alternatively, you can use a mass-assignment `update()` query.',
    example: `// Method 1: Retrieve and save\n$post = Post::find(1);\n$post->title = 'New Title';\n$post->save();\n\n// Method 2: Mass update query\nPost::where('active', 1)->update(['published' => true]);`,
    category: 'Eloquent ORM',
  },
  {
    name: 'Deleting Models & Soft Deletes',
    description: 'Delete a model using the `delete()` method or `destroy()` for a given primary key. To "soft delete" (set a `deleted_at` timestamp), add the `SoftDeletes` trait to your model.',
    example: `// Hard delete\n$post = Post::find(1);\n$post->delete();\nPost::destroy(1);\n\n// Soft delete (model must use SoftDeletes trait)\nuse Illuminate\\Database\\Eloquent\\SoftDeletes;\nclass Post extends Model {\n  use SoftDeletes;\n}`,
    category: 'Eloquent ORM',
  },
  {
    name: 'One to Many (hasMany, belongsTo)',
    description: 'A one-to-many relationship is used to define relationships where a single model is the parent to one or more child models.',
    example: `// In User model (one user has many posts)\npublic function posts() { return $this->hasMany(Post::class); }\n\n// In Post model (one post belongs to a user)\npublic function user() { return $this->belongsTo(User::class); }`,
    category: 'Eloquent ORM',
  },
  {
    name: 'Many to Many (belongsToMany)',
    description: 'Defines a many-to-many relationship, which requires an intermediate "pivot" table to link the two models.',
    example: `// In User model (user has many roles)\npublic function roles() { return $this->belongsToMany(Role::class); }\n\n// In Role model (role belongs to many users)\npublic function users() { return $this->belongsToMany(User::class); }`,
    category: 'Eloquent ORM',
  },
  {
    name: 'Eager Loading (with())',
    description: 'Solves the "N+1 query problem" by loading relationships at the same time as the parent model. This is much more performant than lazy loading.',
    example: `// Instead of this (N+1 problem):\n$books = Book::all();\nforeach ($books as $book) {\n    echo $book->author->name; // Separate query for each author\n}\n\n// Use this:\n$books = Book::with('author')->get();`,
    category: 'Eloquent ORM',
  },
  {
    name: 'Querying Relationships (whereHas)',
    description: 'The `whereHas` method allows you to query for the existence of a relationship with constraints.',
    example: `// Get all posts that have at least one comment\n$postsWithComments = Post::whereHas('comments')->get();\n\n// Get posts that have comments with a specific word\n$posts = Post::whereHas('comments', function ($query) {\n    $query->where('content', 'like', '%foo%');\n})->get();`,
    category: 'Eloquent ORM',
  },
  {
    name: 'Accessors & Mutators',
    description: 'Accessors format attributes when you retrieve them from a model. Mutators transform attributes before saving them to the database. Defined in Laravel 10+ with the `Attribute` class.',
    example: `// In User model\nuse Illuminate\\Database\\Eloquent\\Casts\\Attribute;\n\nprotected function firstName(): Attribute\n{\n    return Attribute::make(\n        get: fn ($value) => ucfirst($value), // Accessor\n        set: fn ($value) => strtolower($value), // Mutator\n    );\n}`,
    category: 'Eloquent ORM',
  },
  {
    name: 'Attribute Casting ($casts)',
    description: 'The `$casts` property on your model provides a convenient method of converting attributes to common data types, or even custom classes.',
    example: `// In Post model\nprotected $casts = [\n    'is_published' => 'boolean',\n    'settings' => 'array', // Will be automatically JSON encoded/decoded\n    'published_at' => 'datetime',\n];`,
    category: 'Eloquent ORM',
  },
  {
    name: 'Local Scopes',
    description: 'Reusable query constraints that can be chained to your Eloquent queries. Scope methods should be prefixed with `scope`.',
    example: `// In Post model\npublic function scopePublished($query)\n{\n    return $query->where('is_published', true);\n}\n\n// Usage:\n$publishedPosts = Post::published()->get();`,
    category: 'Eloquent ORM',
  },

  // --- Database & Migrations ---
  {
    name: 'Schema::create()',
    description: 'Used within a migration file\'s `up` method to define the structure of a new database table.',
    example: `// In a migration file\nSchema::create('posts', function (Blueprint $table) {\n  $table->id();\n  $table->string('title');\n  $table->text('content');\n  $table->timestamps();\n});`,
    category: 'Database & Migrations',
  },
  {
    name: 'Schema::table()',
    description: 'Used to modify an existing table. You can add, change, or drop columns.',
    example: `Schema::table('users', function (Blueprint $table) {\n    $table->string('phone')->nullable(); // Add a column\n    $table->renameColumn('name', 'full_name'); // Rename a column\n    $table->dropColumn('votes'); // Drop a column\n});`,
    category: 'Database & Migrations',
  },
   {
    name: 'Common Column Types',
    description: 'A selection of common column types available through the Schema builder.',
    example: `$table->id(); // Big-incrementing integer (primary key)\n$table->string('name', 100);\n$table->text('description');\n$table->integer('votes');\n$table->boolean('is_active');\n$table->timestamp('published_at')->nullable();\n$table->timestamps(); // Adds created_at and updated_at`,
    category: 'Database & Migrations',
  },
  {
    name: 'Column Modifiers',
    description: 'Chainable methods that can be added to column definitions to alter their properties.',
    example: `$table->string('email')->unique();\n$table->string('role')->default('user');\n$table->text('bio')->nullable();\n$table->string('api_key')->after('email');`,
    category: 'Database & Migrations',
  },
  {
    name: 'Foreign Keys',
    description: 'Define foreign key constraints for your tables. The `constrained` method infers the table and column name.',
    example: `// Long form\n$table->foreign('user_id')->references('id')->on('users');\n\n// Short form (recommended)\n$table->foreignId('user_id')->constrained();`,
    category: 'Database & Migrations',
  },
   {
    name: 'Migration Commands',
    description: 'Common Artisan commands for managing your database schema migrations.',
    example: `php artisan migrate:status // View status of all migrations\nphp artisan migrate:rollback // Roll back the last migration\nphp artisan migrate:rollback --step=5 // Roll back the last 5 migrations`,
    category: 'Database & Migrations',
  },
  {
    name: 'Query Builder',
    description: 'Provides a convenient, fluent interface to create and run database queries. Accessed via the `DB` facade.',
    example: `use Illuminate\\Support\\Facades\\DB;\n\n$users = DB::table('users')->where('votes', '>', 100)->get();`,
    category: 'Database & Migrations',
  },
  {
    name: 'Retrieving Rows (Query Builder)',
    description: 'Use various methods to retrieve data. `get()` returns a collection, `first()` returns a single object, `find()` retrieves a row by its ID, `pluck()` gets a single column\'s values, and `value()` retrieves a single value.',
    example: `$user = DB::table('users')->where('name', 'John')->first();\n$user = DB::table('users')->find(3);\n$titles = DB::table('posts')->pluck('title');\n$email = DB::table('users')->where('name', 'John')->value('email');`,
    category: 'Database & Migrations',
  },
  {
    name: 'Joins (Query Builder)',
    description: 'Perform SQL joins to query related data from multiple tables. Other joins like `leftJoin` and `rightJoin` are also available.',
    example: `DB::table('users')\n    ->join('contacts', 'users.id', '=', 'contacts.user_id')\n    ->select('users.*', 'contacts.phone')\n    ->get();`,
    category: 'Database & Migrations',
  },
  {
    name: 'Insert, Update, Delete (Query Builder)',
    description: 'Methods for inserting, updating, and deleting records in the database.',
    example: `DB::table('users')->insert(['email' => 'a@b.com']);\n\nDB::table('users')->where('id', 1)->update(['votes' => 1]);\n\nDB::table('users')->where('id', 1)->delete();`,
    category: 'Database & Migrations',
  },
   {
    name: 'Database Seeding',
    description: 'Seed your database with test data. Seeders typically use Model Factories to generate large amounts of fake data.',
    example: `// In database/seeders/UserSeeder.php\nuse App\\Models\\User;\n\npublic function run(): void\n{\n    User::factory(10)->create(); // Create 10 fake users\n}`,
    category: 'Database & Migrations',
  },
  {
    name: 'Raw SQL Queries',
    description: 'Run raw SQL statements when the Query Builder is not sufficient. Use `?` bindings to protect against SQL injection.',
    example: `$users = DB::select('select * from users where active = ?', [1]);\n\nDB::statement('drop table users');`,
    category: 'Database & Migrations',
  },
  
  // --- Validation ---
  {
    name: 'Request Validation (Controller)',
    description: 'The easiest way to validate incoming HTTP request data is using the `validate` method on the `Request` object within a controller.',
    example: `public function store(Request $request)\n{\n  $validated = $request->validate([\n    'title' => 'required|unique:posts|max:255',\n    'body' => 'required',\n  ]);\n\n  // The blog post is valid...\n}`,
    category: 'Validation',
  },
  {
    name: 'Form Request Validation (Class)',
    description: 'For more complex scenarios, create a dedicated Form Request class using `php artisan make:request`. This class contains both authorization and validation logic, keeping your controller clean.',
    example: `// In app/Http/Requests/StorePostRequest.php\npublic function rules(): array\n{\n    return [\n        'title' => 'required|unique:posts|max:255',\n        'body' => 'required',\n    ];\n}\n\n// In your controller:\npublic function store(StorePostRequest $request)\n{\n    // Validation is automatic. Access data with $request->validated();\n}`,
    category: 'Validation',
  },
  {
    name: 'Manual Validation',
    description: 'Create a validator instance manually using the `Validator` facade. This is useful outside of HTTP requests (e.g., in Artisan commands).',
    example: `use Illuminate\\Support\\Facades\\Validator;\n\n$validator = Validator::make($data, [\n    'name' => 'required|string|max:255',\n]);\n\nif ($validator->fails()) {\n    // Handle errors...\n}`,
    category: 'Validation',
  },
  {
    name: 'Displaying Errors in Blade',
    description: 'Laravel automatically flashes validation errors to the session. You can access them in your Blade templates via the `$errors` variable and the `@error` directive.',
    example: `<input type="text" name="title" class="@error('title') is-invalid @enderror">\n\n@error('title')\n    <div class="alert alert-danger">{{ $message }}</div>\n@enderror`,
    category: 'Validation',
  },
  {
    name: 'Common Validation Rules',
    description: 'A few of the many available validation rules. They are separated by a pipe `|` character.',
    example: `'email' => 'required|email|unique:users'\n'password' => 'required|min:8|confirmed'\n'avatar' => 'nullable|image|mimes:jpeg,png|max:2048'`,
    category: 'Validation',
  },
  {
    name: 'The "Bail" Rule',
    description: 'Stops running validation rules on an attribute after the first validation failure. This prevents subsequent rules from running on invalid data.',
    example: `'title' => 'bail|required|unique:posts|max:255'`,
    category: 'Validation',
  },
  {
    name: 'Array Validation',
    description: 'Validate nested array input fields using "dot" notation. The asterisk `*` is a wildcard for numeric array keys.',
    example: `[\n    'person.*.email' => 'required|email',\n    'person.*.name' => 'required|string',\n]`,
    category: 'Validation',
  },
  {
    name: 'Conditional Rules',
    description: 'Apply rules based on certain conditions. `sometimes` applies rules only if the field is present in the input. `required_if` makes a field required if another field has a specific value.',
    example: `'play_game' => 'required|boolean',\n'reason' => 'required_if:play_game,true',\n'email' => 'sometimes|required|email',`,
    category: 'Validation',
  },
  {
    name: 'Custom Validation Rules',
    description: 'Create custom validation logic by creating a new Rule object using `php artisan make:rule`. This is useful for complex, reusable validation.',
    example: `// Run: php artisan make:rule Uppercase\n\n// In app/Rules/Uppercase.php\npublic function passes($attribute, $value)\n{\n    return strtoupper($value) === $value;\n}\n\n// Usage in validation array:\n'name' => ['required', new Uppercase],`,
    category: 'Validation',
  },
  
  // --- Authentication ---
  {
    name: 'Authentication Scaffolding',
    description: 'Laravel provides starter kits like Breeze and Jetstream to quickly scaffold a complete authentication system (login, registration, password reset, etc.).',
    example: '// Install Laravel Breeze\ncomposer require laravel/breeze --dev\nphp artisan breeze:install',
    category: 'Authentication',
  },
  {
    name: 'Auth::attempt()',
    description: 'Attempts to authenticate a user using the given credentials. It automatically hashes the password for comparison. It can also handle "remember me" functionality.',
    example: `use Illuminate\\Support\\Facades\\Auth;\n\n$credentials = ['email' => $request->email, 'password' => $request->password];\n\nif (Auth::attempt($credentials, $remember = true)) {\n    $request->session()->regenerate();\n    return redirect()->intended('dashboard');\n}`,
    category: 'Authentication',
  },
  {
    name: 'Auth::check()',
    description: 'The `Auth` facade can be used to check if the current user is authenticated.',
    example: `use Illuminate\\Support\\Facades\\Auth;\n\nif (Auth::check()) {\n    // The user is logged in...\n}`,
    category: 'Authentication',
  },
  {
    name: 'Auth::user()',
    description: 'Retrieves the currently authenticated user model instance.',
    example: `$user = Auth::user();\n$email = Auth::user()->email;`,
    category: 'Authentication',
  },
  {
    name: 'Auth::id()',
    description: 'A quick way to get the ID of the currently authenticated user without fetching the entire user model.',
    example: `$userId = Auth::id();\n// Equivalent to: auth()->id()`,
    category: 'Authentication',
  },
  {
    name: 'auth() Helper',
    description: 'The `auth()` helper function is a convenient alternative to the `Auth` facade.',
    example: `if (auth()->check()) {\n    $user = auth()->user();\n}`,
    category: 'Authentication',
  },
  {
    name: 'Auth::logout()',
    description: 'Logs the user out of the application. This will invalidate their session and regenerate their CSRF token.',
    example: `public function logout(Request $request)\n{\n    Auth::logout();\n    $request->session()->invalidate();\n    $request->session()->regenerateToken();\n    return redirect('/');\n}`,
    category: 'Authentication',
  },
  {
    name: 'Protecting Routes',
    description: 'Use the `auth` middleware to require authentication for a route or group of routes.',
    example: `// Single Route\nRoute::get('/profile', [ProfileController::class, 'edit'])->middleware('auth');\n\n// Group of Routes\nRoute::middleware(['auth'])->group(function () {\n    Route::get('/dashboard', ...);\n});`,
    category: 'Authentication',
  },
  {
    name: 'Authentication Guards',
    description: 'Guards define how users are authenticated for each request. You can specify a guard (e.g., "web" for sessions, "api" for tokens) when using Auth methods.',
    example: `// Authenticate using the 'api' guard\nif (Auth::guard('api')->check()) { ... }\n\n// Get the user from the 'api' guard\n$user = Auth::guard('api')->user();`,
    category: 'Authentication',
  },
  {
    name: 'Gates (Authorization)',
    description: 'Gates are a simple, closure-based way to define authorization rules. They are typically defined in the `AuthServiceProvider`.',
    example: `// In AuthServiceProvider.php\nuse Illuminate\\Support\\Facades\\Gate;\n\nGate::define('update-post', function (User $user, Post $post) {\n    return $user->id === $post->user_id;\n});\n\n// In a controller or Blade view\nif (Gate::allows('update-post', $post)) { ... }`,
    category: 'Authentication',
  },
  {
    name: 'Policies (Authorization)',
    description: 'Policies are classes that group authorization logic for a particular model. They provide a structured way to handle permissions. Generate with `php artisan make:policy`.',
    example: `// In PostPolicy.php\npublic function update(User $user, Post $post): bool\n{\n    return $user->id === $post->user_id;\n}\n\n// In a controller\n$this->authorize('update', $post);`,
    category: 'Authentication',
  },

  // --- Collections ---
  {
    name: 'collect()',
    description: 'The `collect` helper creates a new collection instance from the given array.',
    example: `$collection = collect([1, 2, 3]);`,
    category: 'Collections',
  },
  {
    name: 'map()',
    description: 'Iterates through the collection and passes each value to a callback. The callback\'s return value replaces the original item.',
    example: `$doubled = collect([1, 2, 3])->map(fn ($item) => $item * 2);\n// [2, 4, 6]`,
    category: 'Collections',
  },
  {
    name: 'filter()',
    description: 'Filters the collection using a callback. Only items for which the callback returns `true` are kept.',
    example: `$evens = collect([1, 2, 3, 4])->filter(fn ($item) => $item % 2 == 0);\n// [2, 4]`,
    category: 'Collections',
  },
  {
    name: 'pluck()',
    description: 'Retrieves all of the values for a given key from a collection of objects or arrays.',
    example: `$names = collect([\n    ['product' => 'Desk', 'price' => 200],\n    ['product' => 'Chair', 'price' => 100],\n])->pluck('product');\n// ['Desk', 'Chair']`,
    category: 'Collections',
  },
  {
    name: 'each()',
    description: 'Iterates over the items in the collection and passes each item to a callback. Useful for "side effects" without creating a new collection.',
    example: `collect([1, 2, 3])->each(function ($item) {\n    // Send email, log data, etc.\n});`,
    category: 'Collections',
  },
  {
    name: 'reduce()',
    description: 'Reduces the collection to a single value by passing a callback function that accumulates a result.',
    example: `$sum = collect([1, 2, 3])->reduce(fn ($carry, $item) => $carry + $item, 0);\n// 6`,
    category: 'Collections',
  },
  {
    name: 'first() / last()',
    description: '`first()` returns the first element in the collection that passes a given truth test. `last()` returns the last element.',
    example: `$first = collect([1, 2, 3])->first(); // 1\n$lastEven = collect([1, 2, 3, 4])->last(fn ($value) => $value % 2 === 0); // 4`,
    category: 'Collections',
  },
  {
    name: 'groupBy()',
    description: 'Groups the collection\'s items by a given key.',
    example: `$collection = collect([\n    ['account_id' => 'acc-1', 'product' => 'Desk'],\n    ['account_id' => 'acc-1', 'product' => 'Chair'],\n    ['account_id' => 'acc-2', 'product' => 'Bookcase'],\n])->groupBy('account_id');\n// Returns a collection of collections`,
    category: 'Collections',
  },
  {
    name: 'sortBy() / sortByDesc()',
    description: 'Sorts the collection by the given key. `sortByDesc()` sorts in descending order.',
    example: `$sorted = collect([\n    ['name' => 'Desk', 'price' => 200],\n    ['name' => 'Chair', 'price' => 100],\n])->sortBy('price');`,
    category: 'Collections',
  },
  {
    name: 'reject()',
    description: 'The inverse of the `filter` method. It returns all items from the collection that do not pass a given truth test.',
    example: `$collection = collect([1, 2, 3, 4]);\n$filtered = $collection->reject(fn ($value) => $value > 2);\n// [1, 2]`,
    category: 'Collections',
  },
  {
    name: 'unique()',
    description: 'Returns all of the unique items in the collection. You can pass a key to check for uniqueness based on a property.',
    example: `$unique = collect([1, 1, 2, 2, 3])->unique(); // [1, 2, 3]`,
    category: 'Collections',
  },
  {
    name: 'contains()',
    description: 'Determines whether the collection contains a given item. You can also pass a key/value pair or a callback.',
    example: `collect(['name' => 'Desk', 'price' => 100])->contains('Desk'); // true\ncollect([1, 2, 3])->contains(fn ($value) => $value > 2); // true`,
    category: 'Collections',
  },
  {
    name: 'sum() / avg()',
    description: 'Returns the sum or average of all items in the collection. You can pass a key to sum/average a specific property.',
    example: `collect([1, 2, 3])->sum(); // 6\ncollect([['price' => 100], ['price' => 200]])->avg('price'); // 150`,
    category: 'Collections',
  },
  {
    name: 'tap()',
    description: 'Allows you to "tap" into the collection at a specific point in a chain, do something with the items, and then pass the collection along.',
    example: `collect([2, 4, 3, 1])\n    ->sort()\n    ->tap(fn ($collection) => Log::info('Values after sorting', $collection->values()->all()))\n    ->map(...)`,
    category: 'Collections',
  },
  {
    name: 'where()',
    description: 'Filters the collection by a given key / value pair.',
    example: `$products = collect([\n    ['product' => 'Desk', 'price' => 200, 'active' => true],\n    ['product' => 'Chair', 'price' => 100, 'active' => false],\n])->where('active', true);`,
    category: 'Collections',
  },

  // --- File Storage ---
  {
    name: 'Filesystem Configuration',
    description: 'Configure your storage "disks" in `config/filesystems.php`. Disks can be local, public, or cloud-based (like Amazon S3). The `public` disk is for files that should be publicly accessible.',
    example: `// config/filesystems.php
'disks' => [
    'local' => [ 'driver' => 'local', 'root' => storage_path('app'), ],
    'public' => [ 'driver' => 'local', 'root' => storage_path('app/public'), 'url' => env('APP_URL').'/storage', 'visibility' => 'public', ],
    's3' => [ 'driver' => 's3', /* ... */ ],
],`,
    category: 'File Storage',
  },
  {
    name: 'php artisan storage:link',
    description: 'Creates a symbolic link from `public/storage` to `storage/app/public`. This makes files in your `public` disk accessible from the web.',
    example: 'php artisan storage:link',
    category: 'File Storage',
  },
  {
    name: 'Specifying a Disk',
    description: 'By default, the Storage facade uses the `local` disk. You can specify a different disk using the `disk()` method.',
    example: `// Store a file on Amazon S3
Storage::disk('s3')->put('avatars/1.jpg', $fileContent);`,
    category: 'File Storage',
  },
  {
    name: 'Storage::put()',
    description: 'Stores file contents on a disk. If the file already exists, it will be overwritten.',
    example: `use Illuminate\\Support\\Facades\\Storage;\n\nStorage::put('avatars/1.jpg', $fileContent);`,
    category: 'File Storage',
  },
   {
    name: 'File Uploads',
    description: 'Store an uploaded file from a request using the `store` method on the file object. It automatically generates a unique ID for the filename.',
    example: `public function update(Request $request)\n{\n    // Store in 'avatars' directory on the 'public' disk\n    $path = $request->file('avatar')->store('avatars', 'public');\n\n    return $path;\n}`,
    category: 'File Storage',
  },
  {
    name: 'Storage::get()',
    description: 'Retrieves the contents of a file as a string.',
    example: `$contents = Storage::get('file.jpg');`,
    category: 'File Storage',
  },
  {
    name: 'Storage::exists() / missing()',
    description: 'Check if a file exists or is missing from the disk.',
    example: `if (Storage::exists('file.jpg')) { /* ... */ }\n\nif (Storage::missing('file.jpg')) { /* ... */ }`,
    category: 'File Storage',
  },
  {
    name: 'Storage::download()',
    description: 'Generates a response that forces the user\'s browser to download the file at the given path.',
    example: `return Storage::download('photos/me.jpg', 'my_photo.jpg');`,
    category: 'File Storage',
  },
  {
    name: 'Storage::url()',
    description: 'Generates a public URL for a file. This only works for files on the `public` disk (or a cloud disk configured for public access).',
    example: `$url = Storage::url('avatars/1.jpg');`,
    category: 'File Storage',
  },
  {
    name: 'Temporary URLs (Cloud)',
    description: 'For files on cloud storage like S3 with `private` visibility, you can create a temporary, signed URL to grant access for a limited time.',
    example: `$url = Storage::disk('s3')->temporaryUrl(\n    'private/file.jpg',\n    now()->addMinutes(5)\n);`,
    category: 'File Storage',
  },
  {
    name: 'File Metadata',
    description: 'Retrieve metadata about a file, such as its size, last modification time, or MIME type.',
    example: `$size = Storage::size('file.jpg'); // in bytes\n$lastModified = Storage::lastModified('file.jpg'); // unix timestamp`,
    category: 'File Storage',
  },
  {
    name: 'Storage::delete()',
    description: 'Deletes a single file or an array of files from the disk.',
    example: `Storage::delete('file.jpg');\n\nStorage::delete(['file1.jpg', 'file2.jpg']);`,
    category: 'File Storage',
  },
  {
    name: 'Directory Management',
    description: 'Methods for creating, deleting, and listing the contents of directories.',
    example: `// Create a directory\nStorage::makeDirectory('my_dir');\n\n// Delete a directory (and all its files)\nStorage::deleteDirectory('my_dir');\n\n// Get all files in a directory\n$files = Storage::files('my_dir');\n\n// Get all files recursively\n$allFiles = Storage::allFiles('my_dir');`,
    category: 'File Storage',
  },

  // --- Queues ---
  {
    name: 'Creating Jobs',
    description: 'Generate a new queueable job class using the `make:job` Artisan command.',
    example: 'php artisan make:job ProcessPodcast',
    category: 'Queues',
  },
  {
    name: 'Job Structure',
    description: 'The job\'s logic is placed within the `handle` method. Any Eloquent models are automatically serialized and deserialized.',
    example: `class ProcessPodcast implements ShouldQueue\n{\n    use InteractsWithQueue, Queueable, SerializesModels;\n\n    public function __construct(protected Podcast $podcast) {}\n\n    public function handle(): void\n    {\n        // Process the uploaded podcast...\n    }\n}`,
    category: 'Queues',
  },
  {
    name: 'Dispatching Jobs',
    description: 'Dispatch a job to the queue using the static `dispatch` method on the job class.',
    example: `use App\\Jobs\\ProcessPodcast;\n\nProcessPodcast::dispatch($podcast);`,
    category: 'Queues',
  },
  {
    name: 'Delayed Dispatching',
    description: 'The `delay` method allows you to specify that a job should not be made available to a worker until a certain number of seconds, or a `DateTime` instance, has passed.',
    example: `// Dispatch a job for 10 minutes from now\nProcessPodcast::dispatch($podcast)->delay(now()->addMinutes(10));`,
    category: 'Queues',
  },
  {
    name: 'Synchronous Dispatching',
    description: 'The `dispatchSync` method will execute the job immediately in the current process. This is useful for testing or for jobs that must run synchronously.',
    example: `// This job will not be sent to the queue\nProcessPodcast::dispatchSync($podcast);`,
    category: 'Queues',
  },
  {
    name: 'Specifying the Queue',
    description: 'Push a job onto a specific queue using the `onQueue` method. You can also specify the connection with `onConnection`.',
    example: `// Dispatch to the 'videos' queue on the 'redis' connection\nProcessPodcast::dispatch($podcast)\n    ->onQueue('videos')\n    ->onConnection('redis');`,
    category: 'Queues',
  },
  {
    name: 'Job Chaining',
    description: 'The `Bus` facade\'s `chain` method allows you to specify a list of queued jobs that should be run in sequence. If one job in the sequence fails, the rest will not be run.',
    example: `use Illuminate\\Support\\Facades\\Bus;\n\nBus::chain([\n    new FetchPodcast(),\n    new ProcessPodcast(),\n    new PublishPodcast(),\n])->dispatch();`,
    category: 'Queues',
  },
  {
    name: 'Running the Queue Worker',
    description: 'The `queue:work` Artisan command starts a worker that will process new jobs as they are pushed onto the queue. It will continue to run until it is manually stopped or the terminal is closed.',
    example: `// Process jobs on the default queue\nphp artisan queue:work\n\n// Process jobs from specific queues\nphp artisan queue:work --queue=high,default\n\n// Process one job and then exit\nphp artisan queue:work --once`,
    category: 'Queues',
  },
  {
    name: 'Failed Jobs',
    description: 'When a queued job fails, it is inserted into the `failed_jobs` database table. You can retry a failed job using the `queue:retry` command.',
    example: `// Create the failed_jobs table migration\nphp artisan queue:failed-table\nphp artisan migrate\n\n// Retry a specific failed job by its ID\nphp artisan queue:retry <ID>\n\n// Retry all failed jobs\nphp artisan queue:retry --all`,
    category: 'Queues',
  },
  {
    name: 'Queue Configuration',
    description: 'The queue configuration is stored in `config/queue.php`. The default connection is set in your `.env` file via the `QUEUE_CONNECTION` variable. Common drivers are `sync` (for local development), `database`, `redis`, and `sqs`.',
    example: `// .env file\nQUEUE_CONNECTION=redis`,
    category: 'Queues',
  },

  // --- Task Scheduling ---
  {
    name: 'Defining Schedules',
    description: 'Define all of your scheduled tasks in the `schedule` method of the `app/Console/Kernel.php` class.',
    example: `// app/Console/Kernel.php\nprotected function schedule(Schedule $schedule): void\n{\n    $schedule->command('emails:send')->daily();\n}`,
    category: 'Task Scheduling',
  },
  {
    name: 'Running The Scheduler',
    description: 'A single cron entry on your server is needed to run all of Laravel\'s scheduled tasks every minute.',
    example: `* * * * * cd /path-to-your-project && php artisan schedule:run >> /dev/null 2>&1`,
    category: 'Task Scheduling',
  },
  {
    name: 'Scheduling Artisan Commands',
    description: 'Schedule an Artisan command to run. You can pass arguments and options to the command as an array.',
    example: `// Simple command\n$schedule->command('emails:send')->daily();\n\n// Command with arguments\n$schedule->command('publish:posts', ['--force'])->hourly();`,
    category: 'Task Scheduling',
  },
  {
    name: 'Scheduling Queued Jobs',
    description: 'Schedule a queued job to be dispatched. This provides a clean way to schedule complex, long-running tasks without blocking the scheduler.',
    example: `$schedule->job(new ProcessPodcast)->everyMinute();`,
    category: 'Task Scheduling',
  },
  {
    name: 'Scheduling Shell Commands',
    description: 'Use the `exec` method to schedule a command-line program.',
    example: `$schedule->exec('node /path/to/script.js')->daily();`,
    category: 'Task Scheduling',
  },
  {
    name: 'Common Schedule Frequencies',
    description: 'Laravel provides a fluent, expressive API for scheduling tasks to run at various frequencies.',
    example: `$schedule->command('report')->everyMinute();\n$schedule->command('report')->everyFiveMinutes();\n$schedule->command('report')->hourly();\n$schedule->command('backup')->dailyAt('01:00');\n$schedule->command('report')->weekly();\n$schedule->command('report')->monthlyOn(4, '15:00');\n$schedule->command('report')->quarterly();\n$schedule->command('report')->yearly();`,
    category: 'Task Scheduling',
  },
  {
    name: 'Day Constraints',
    description: 'You can constrain a task to run only on specific days of the week.',
    example: `$schedule->command('report')->weekdays(); // Mon-Fri\n$schedule->command('report')->weekends(); // Sat-Sun\n$schedule->command('report')->mondays();\n$schedule->command('report')->tuesdays()->at('13:00');`,
    category: 'Task Scheduling',
  },
  {
    name: 'Time & Truth Constraints',
    description: 'You can constrain a task to run only during specific time windows using `between` or a truth test with `when`.',
    example: `// Run between 8 AM and 5 PM\n$schedule->command('email:send')->hourly()->between('8:00', '17:00');\n\n// Run only if a condition is true\n$schedule->command('backup')->daily()->when(function () {\n    return true; // Your logic here\n});`,
    category: 'Task Scheduling',
  },
  {
    name: 'Preventing Task Overlaps',
    description: 'The `withoutOverlapping()` method ensures that a long-running task does not start again if the previous instance is still running.',
    example: `$schedule->command('report:generate')->everyMinute()->withoutOverlapping();`,
    category: 'Task Scheduling',
  },
  {
    name: 'Running on One Server',
    description: 'When running the scheduler on multiple servers, `onOneServer()` ensures a task only runs on a single server. This requires a cache driver like Redis or Memcached.',
    example: `$schedule->command('report:generate')->fridays()->at('17:00')->onOneServer();`,
    category: 'Task Scheduling',
  },
  {
    name: 'Task Output',
    description: 'You can route the output of a scheduled task to a file for later inspection. `appendOutputTo` appends, while `sendOutputTo` overwrites.',
    example: `$schedule->command('inspire')\n         ->everyMinute()\n         ->sendOutputTo('/storage/logs/inspire.log');`,
    category: 'Task Scheduling',
  },
  {
    name: 'Task Hooks',
    description: 'Use `before`, `after`, `onSuccess`, and `onFailure` to execute code before or after the scheduled task runs.',
    example: `$schedule->command('backup:run')\n         ->daily()\n         ->before(fn () => info('Starting backup...'))\n         ->onSuccess(fn () => info('Backup successful!'))\n         ->onFailure(fn () => info('Backup failed!'));`,
    category: 'Task Scheduling',
  },
    {
    name: 'Maintenance Mode',
    description: 'Scheduled tasks will not run when the application is in maintenance mode. To force a task to run even in maintenance mode, use the `evenInMaintenanceMode()` method.',
    example: `$schedule->command('heartbeat')->everyMinute()->evenInMaintenanceMode();`,
    category: 'Task Scheduling',
  },
  {
    name: 'php artisan schedule:list',
    description: 'Lists all of the scheduled tasks that are configured in your application along with their next scheduled run time.',
    example: 'php artisan schedule:list',
    category: 'Task Scheduling',
  },

  // --- Testing ---
  {
    name: 'php artisan test',
    description: 'Runs your application\'s feature and unit tests. Accepts most PHPUnit/Pest arguments, like `--filter` to run a specific test.',
    example: 'php artisan test\nphp artisan test --filter="UserTest"',
    category: 'Testing',
  },
  {
    name: 'Creating Tests',
    description: 'Generate a new test class using the `make:test` Artisan command. Feature tests are in `tests/Feature`, Unit tests are in `tests/Unit`.',
    example: 'php artisan make:test PostTest --unit',
    category: 'Testing',
  },
  {
    name: 'Feature Test Example',
    description: 'Feature tests make HTTP requests to your application and inspect the output.',
    example: `// tests/Feature/HomepageTest.php\npublic function test_the_application_returns_a_successful_response(): void\n{\n    $response = $this->get('/');\n\n    $response->assertStatus(200);\n    $response->assertSee('Welcome');\n}`,
    category: 'Testing',
  },
  {
    name: 'Common HTTP Assertions',
    description: 'Laravel provides many fluent assertion methods for inspecting responses.',
    example: `$response->assertStatus(200);\n$response->assertOk(); // Alias for 200\n$response->assertNotFound(); // 404\n$response->assertSee('Some Text');\n$response->assertJson(['key' => 'value']);\n$response->assertRedirect('/login');`,
    category: 'Testing',
  },
  {
    name: 'actingAs() Helper',
    description: 'Authenticates a given user as the current user for the test. Useful for testing routes protected by authentication middleware.',
    example: `$user = User::factory()->create();\n\n$this->actingAs($user)\n     ->get('/dashboard')\n     ->assertStatus(200);`,
    category: 'Testing',
  },
  {
    name: 'RefreshDatabase Trait',
    description: 'A trait to automatically reset your database after each test. It runs migrations and wraps each test in a database transaction.',
    example: `use Illuminate\\Foundation\\Testing\\RefreshDatabase;\n\nclass UserTest extends TestCase\n{\n    use RefreshDatabase;\n\n    // Your tests...\n}`,
    category: 'Testing',
  },
  {
    name: 'Model Factories',
    description: 'Use model factories to easily generate fake model data for testing or seeding your database.',
    example: `use App\\Models\\User;\n\n// Create a single user\n$user = User::factory()->create();\n\n// Create three users\n$users = User::factory()->count(3)->create();\n\n// Create a user with specific attributes\n$user = User::factory()->create(['is_admin' => true]);`,
    category: 'Testing',
  },
  {
    name: 'Database Assertions',
    description: 'Methods to assert that data exists (or is missing) in your database after performing an action.',
    example: `$this->assertDatabaseHas('users', [\n    'email' => 'sally@example.com',\n]);\n\n$this->assertDatabaseMissing('users', [\n    'email' => 'george@example.com',\n]);`,
    category: 'Testing',
  },
  {
    name: 'Advanced JSON Assertions',
    description: 'Provides a fluent API for asserting the structure and content of JSON API responses.',
    example: `$response->assertJsonStructure([\n    'data' => [\n        '*' => ['id', 'name', 'email']\n    ]\n]);\n\n$response->assertJsonFragment(['name' => 'Steve']);`,
    category: 'Testing',
  },
  {
    name: 'Validation Assertions',
    description: 'Methods to assert that a request failed validation for specific fields, or check for specific error messages.',
    example: `$response = $this->post('/posts', ['title' => '']);\n\n$response->assertSessionHasErrors('title');\n\n// Assert a specific error message\n$response->assertSessionHasErrors(['title' => 'The title field is required.']);`,
    category: 'Testing',
  },
  {
    name: 'Facade Mocking',
    description: 'Mocking allows you to test parts of your application in isolation by replacing complex dependencies with a simple "mock" object.',
    example: `use Illuminate\\Support\\Facades\\Cache;\n\nCache::shouldReceive('get')\n     ->once()\n     ->with('key')\n     ->andReturn('value');`,
    category: 'Testing',
  },
  {
    name: 'Event / Mail / Notification Faking',
    description: 'Laravel provides helpers to "fake" events, mail, notifications, and queued jobs, allowing you to assert they were dispatched without actually executing them.',
    example: `use Illuminate\\Support\\Facades\\Mail;\nuse App\\Mail\\OrderShipped;\n\nMail::fake();\n\n// Perform order shipping logic...\n\nMail::assertSent(OrderShipped::class);`,
    category: 'Testing',
  },
  {
    name: 'Console Command Testing',
    description: 'Test your custom Artisan commands using the `artisan` method. You can provide input and make assertions on the output.',
    example: `$this->artisan('send:emails --all')\n     ->expectsOutput('All emails sent!')\n     ->assertExitCode(0);`,
    category: 'Testing',
  },
];